git clone https://github.com/yongjian-h/yongjian.git

git status 

git add date.txt	//将文件存入缓存区

git reset HEAD date.txt    //将文件撤出缓存区

git commit 			//提交到本地工作区 会出现一个修改的评论，写为什么要修改
				//nothing to commit (working directory clean)

git push			//上传到github